language: python
python: '3.6'
install:
  - sudo apt-get update
  - if [[ "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
      wget https://repo.continuum.io/miniconda/Miniconda2-latest-Linux-x86_64.sh -O miniconda.sh;
    else
      wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh;
    fi
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  - conda info -a
  # Replace dep1 dep2 ... with your dependencies
  - conda config --add channels intel
  - conda create -q -n test-environment -c intel/label/test intelpython r rpy2 r-feather pandas numba openmp cython pip scikit-learn
  - source activate test-environment
  - conda install -c intel/label/test --file requirements.txt
  - pip install -r requirements-noconda.txt
  - pip install -r requirements-dev.txt
  - R --vanilla -e 'install.packages("survey", repos="http://R-Forge.R-project.org")'
  - pip install -e .
script:
  - pytest
notifications:
  email:
    on_success: never
    on_failure: always
